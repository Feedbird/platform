import { Service } from "@/lib/supabase/client";
import React from "react";

type Props = {
  service: Service;
};

export default function ServiceSwitchCard({ service }: Props) {
  return <div>ServiceSwitchCard</div>;
}
